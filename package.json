{
  "name": "tws-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Tiny Whoop Staffordshire club website (Astro). Events synced from the iFPV calendar at build time.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "deploy": "npm run build && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "12",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^5.6.0"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
